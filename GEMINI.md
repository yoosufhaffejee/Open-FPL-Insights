# Development Workflow Rules

## Verification & Syntax Checking
- **Always** run a compiler, syntax check, or linter on modified files before completing your turn. 
- For JavaScript files, use `node -c <filename>` or a similar tool to verify syntax integrity.
- Never end a turn after modifying source code without first verifying that the changes did not introduce syntax errors.
